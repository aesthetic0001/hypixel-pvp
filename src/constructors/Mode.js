function Mode(name, description, onHypixel, onLocalHost) {
    this.name = name
    this.description = description
    this.hypixel = onHypixel
    this.localhost = onLocalHost
}

module.exports = {
    Mode
}